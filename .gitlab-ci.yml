image: 
  name: node:latest
  name: docker/compose:latest

cache:
  paths:
    - node_modules/

test:
    script:
    - npm install
    - cd ./cache/
    - docker-compose down
    - docker-compose build
    - docker-compose up -d
    - cd ..
    - npm run test
