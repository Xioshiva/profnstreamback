build:cache:
  image: docker/compose:latest
  script:
    - cd ./cache/
    - docker-compose down
    - docker-compose build
    - docker-compose up -d

build:test:
  image: node:latest
  script:
    - npm install
    - npm run test
